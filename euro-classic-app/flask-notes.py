"""

In python, a subdirectory that includes a __init__.py
file is considred a package and can be imported

When package is imported, the __init__.py executes and defines what symbols
the package exposes to the outside world

"""